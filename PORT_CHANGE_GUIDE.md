# 端口更改指南 - 解决移动设备连接问题

## 🚨 问题描述

移动设备（iPhone/华为）无法连接后端，显示 `TypeError: Failed to fetch` 错误。

## 🔍 问题分析

- **桌面设备**（MacBook/iPad）：正常连接 ✅
- **移动设备**（iPhone/华为）：连接失败 ❌
- **错误类型**：`TypeError: Failed to fetch`
- **根本原因**：移动网络阻止非标准端口8000

## 🛠️ 解决方案

### 端口更改：8000 → 8080

**原因**：
- 端口8000被移动网络阻止
- 端口8080通常被移动网络允许
- 不需要管理员权限

**更改内容**：
1. 后端端口：`8000` → `8080`
2. 前端配置：自动检测新端口

## 📋 实施步骤

### 步骤1：修改后端配置 ✅

```bash
# 文件：realm_of_balance_backend/app/config/settings.py
PORT: int = 8080  # 从8000改为8080
```

### 步骤2：更新前端配置 ✅

```typescript
// 文件：Realm_of_Balance_App_frontend/config/environment.ts
'183.173.136.51': {
  backend: {
    host: '183.173.136.51',
    port: 8080,  // 更新为8080端口
    protocol: 'http'
  }
}
```

### 步骤3：重启后端服务

```bash
cd realm_of_balance_backend
# 停止当前服务
pkill -f "python.*run.py"

# 重新启动
python run.py
```

### 步骤4：测试连接

1. **本地测试**：
   ```bash
   curl http://localhost:8080/health
   ```

2. **网络测试**：
   ```bash
   curl http://183.173.136.51:8080/health
   ```

3. **移动设备测试**：
   - 访问：`http://183.173.136.51:3000`
   - 使用调试组件查看配置
   - 测试功能是否正常

## 🌐 新的访问地址

### 后端API
- **本地**：`http://localhost:8080`
- **网络**：`http://183.173.136.51:8080`

### 前端应用
- **本地**：`http://localhost:3000`
- **网络**：`http://183.173.136.51:3000`

## 🔧 验证配置

### 1. 检查后端状态
```bash
# 检查端口是否监听
netstat -tlnp | grep 8080

# 测试健康检查
curl http://localhost:8080/health
```

### 2. 检查前端配置
- 访问调试组件
- 确认后端地址显示为8080端口
- 测试后端连接

### 3. 移动设备测试
- 使用诊断工具：`/mobile-debug.html`
- 运行完整网络诊断
- 确认8080端口连接成功

## 📱 移动设备优化

### 端口选择优先级
1. **端口80**：最佳兼容性（需要管理员权限）
2. **端口443**：HTTPS标准端口（需要SSL证书）
3. **端口8080**：平衡兼容性和权限要求 ⭐
4. **端口8000**：开发端口（移动网络可能阻止）

### 网络环境测试
- **WiFi网络**：通常限制较少
- **4G/5G网络**：可能有端口限制
- **不同运营商**：限制策略可能不同

## 🚀 长期优化建议

### 1. 使用HTTPS
- 配置SSL证书
- 使用端口443
- 提高安全性和兼容性

### 2. 反向代理
- 使用Nginx
- 统一端口管理
- 更好的负载均衡

### 3. 云服务部署
- 使用标准端口
- 自动SSL配置
- 更好的全球访问性

## 🔍 故障排除

### 如果8080端口仍然不工作

1. **检查防火墙**：
   ```bash
   sudo ufw status
   sudo ufw allow 8080
   ```

2. **检查服务状态**：
   ```bash
   ps aux | grep python
   netstat -tlnp | grep 8080
   ```

3. **检查日志**：
   ```bash
   tail -f logs/app.log
   ```

### 如果仍然无法连接

1. **使用端口扫描工具**确认端口状态
2. **检查移动网络运营商**是否有特殊限制
3. **考虑使用端口80**（需要管理员权限）

## 📞 获取帮助

如果问题仍然存在：
1. 运行移动设备诊断工具
2. 收集详细的错误信息
3. 检查服务器日志
4. 联系网络管理员

## ✅ 预期结果

端口更改后：
- 移动设备应该能正常连接后端
- 桌面设备继续正常工作
- 所有功能正常运行
- 网络访问体验改善
