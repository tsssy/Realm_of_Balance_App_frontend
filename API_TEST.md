# API测试说明

## 当前状态
✅ 页面已经恢复正常显示
✅ API集成功能已逐步引入
✅ 用户信息收集功能正常

## 新增功能

### 1. 自动设备ID生成
- 应用启动时自动生成唯一设备ID
- 存储在localStorage中，用于用户识别

### 2. 后端连接测试
- 应用启动时自动测试后端连接
- 如果后端不可用，会显示警告但继续运行

### 3. 用户状态检查
- 尝试检查用户是否为新用户
- 如果是老用户，自动填充用户信息

### 4. 用户创建API调用
- 点击"Complete Blueprint"时调用用户创建API
- 显示加载状态和进度提示
- 创建成功后保存用户ID

## 测试步骤

### 1. 基础功能测试
- [x] 引导页面正常显示
- [x] 出生时间选择页面正常显示
- [x] 出生地点选择页面正常显示
- [x] 页面跳转正常

### 2. API功能测试
1. **启动应用** - 查看控制台输出
   - 应该看到设备ID生成
   - 应该看到后端连接测试结果
   - 应该看到用户状态检查结果

2. **完成用户信息收集**
   - 点击"Begin Journey"
   - 填写出生日期和时间
   - 点击"Continue Journey"
   - 填写出生地点
   - 点击"Complete Blueprint"

3. **观察API调用过程**
   - 按钮应该显示"Creating User..."和加载动画
   - 输入框应该被禁用
   - 控制台应该显示API调用过程

### 3. 控制台预期输出

#### 应用启动时：
```
🔧 生成的设备ID: device_1234567890_abc123
✅ 后端连接成功: {status: "healthy", service: "Realm of Balance App", version: "1.0.0"}
🔧 用户状态检查结果: {success: true, data: {is_new_user: true, has_blueprint: false}}
🔧 新用户，需要创建账户
```

#### 用户创建时：
```
🔧 用户信息收集完成: {gender: 'other', birthDate: '1990-01-01', birthTime: '12:00', birthLocation: 'New York, NY, USA'}
🔧 使用设备ID: device_1234567890_abc123
🔧 发送用户创建请求: {device_id: "...", profile: {...}}
🎉 用户创建成功: {user_id: "...", device_id: "...", ...}
🔧 用户ID已保存: ...
```

## 错误处理

### 1. 后端不可用
如果后端服务未启动，会看到：
```
⚠️ 后端连接失败，继续使用本地模式: Error: fetch failed
🔧 用户状态检查失败，继续使用本地模式: Error: ...
```

### 2. API调用失败
如果用户创建失败，会看到：
```
❌ 创建用户失败: Error: ...
🔧 继续使用本地模式，跳转到元素分析页面
```

## 下一步测试

### 1. 确保后端运行
```bash
# 在后端目录中运行
cd ../realm_of_balance_backend
python run.py
```

### 2. 测试完整流程
1. 启动前端应用
2. 完成用户注册流程
3. 检查控制台输出
4. 验证用户是否成功创建

### 3. 检查数据库
在后端控制台中查看用户是否成功保存到数据库

## 注意事项
- 保持前端视觉效果不变
- API调用失败时会自动降级到本地模式
- 所有操作都有详细的日志记录
- 加载状态提供良好的用户体验
